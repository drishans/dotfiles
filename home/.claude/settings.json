{
  "theme": "dark-ansi",
  "statusLine": {
    "type": "command",
    "command": "jq -r '(.model.display_name // \"unknown\") as $model | (.context_window.used_percentage // null) as $used | if $used == null then $model else \"\\($model) | ctx: \\($used | round)% used\" end'"
  }
}